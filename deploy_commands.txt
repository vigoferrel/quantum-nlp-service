# 🚀 COMANDOS DE DESPLIEGUE - QUANTUM SUPREMACY SYSTEM
# ===================================================
# Ejecuta estos comandos uno por uno en la terminal SSH de Hostinger

# 1. Navegar al directorio del sitio
cd public_html

# 2. Verificar archivos existentes
ls -la

# 3. Verificar Python disponible
python3 --version

# 4. Instalar dependencias Flask
pip3 install Flask==2.3.3
pip3 install Werkzeug==2.3.7
pip3 install Jinja2==3.1.2
pip3 install MarkupSafe==2.1.3
pip3 install itsdangerous==2.1.2
pip3 install click==8.1.7
pip3 install blinker==1.6.3

# 5. Configurar permisos de ejecución
chmod +x main.py

# 6. Probar la aplicación Flask
python3 -c "from main import app; print('✅ Aplicación Flask cargada correctamente')"

# 7. Verificar configuración final
ls -la
python3 --version

# 8. Crear archivo WSGI para producción
cat > wsgi.py << 'EOF'
#!/usr/bin/env python3
import sys
import os

# Agregar el directorio actual al path
sys.path.insert(0, os.path.dirname(__file__))

# Importar la aplicación Flask
from main import app

# Configurar para producción
if __name__ == "__main__":
    app.run()
EOF

# 9. Configurar permisos del archivo WSGI
chmod +x wsgi.py

# 10. Verificar que todo funciona
python3 wsgi.py

# 🎉 DESPLIEGUE COMPLETADO!
# Tu sistema estará disponible en:
# - https://vigoleonrocks.com
# - https://vigoleonrocks.com/api/status
# - https://vigoleonrocks.com/api/metrics
